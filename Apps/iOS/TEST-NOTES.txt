本版更新：减少 VPN 连接后退到后台或锁屏时的诊断文件写入，常规检查约每 60 秒一次，重要连接事件仍及时保存；连接质量页优先读取实时统计，读取超时时显示带观测时间的缓存，避免旧数据覆盖新数据。已在真机后台采集中确认高频诊断写入消失，实际节电比例仍需持续测量。

请重点验证：
0. 连接 VPN 后退到后台或锁屏，持续使用后检查连接稳定性和电池用量。
1. VPN 登录、连接和公司内网业务访问。
2. Wi-Fi 与蜂窝网络切换、短时断网后的连接恢复。
3. 开启自动连接后的锁屏恢复，以及手动断开后保持断开。
4. 反复进入连接质量页、切换前后台，确认流量统计更新正常；断开并重新连接后不会出现上次连接的数据。
5. 手机 VPN 连接时的个人热点兼容性，以及首次启动的配置提示。
如遇问题，请记录设备、系统版本、发生时间和网络类型，并通过连接质量页分享诊断信息。
