# Windows 验证记录

## 2026-09-09 · 0.1.3 安装目录运行时编译回归

- 用户安装 0.1.2 后，2026-09-08 23:55:53、23:56:08、23:57:41 三次连接均在 `network.ps1:227` 的 Add-Type 阶段失败。网络记录为 `network.verify / System.Exception / HResult -2146233088`；服务保持 Running，退出后清理成功。
- 原始 PowerShell Operational 4100（RecordId 8480）给出 `SOURCE_CODE_ERROR`：`System.Net.IPAddress` 被转发到 `System.Net.Primitives, Version=0.0.0.0`，缺少编译引用。安装目录存在自包含 .NET 10 `System.dll`，PowerShell 5.1 CodeDOM 的相对引用选中了它。
- 四组对照：相同源码、相同 PS5.1/受限 PATH，在仓库 CWD 编译通过；真实 ProcessStartInfo.WorkingDirectory=安装目录时精确失败；同目录改为绝对 Framework 引用后通过；新 Framework 校验 DLL 与真实 .NET10 System/System.Core/System.Net.Primitives/mscorlib/netstandard 同目录、全新 PS5.1、禁用 Add-Type、TEMP/TMP 指向普通文件时，加载和正常/冲突路由判断通过。此错误普通用户即可复现，不能归因为 SYSTEM 临时目录权限或 CorpLink。
- 0.1.3 去掉连接时的编译器依赖，构建使用绝对 mscorlib/System/System.Core Framework 引用。安装器强制检查新 DLL，缺失或哈希损坏在停服务前失败；网络脚本加载异常使用独立 `route.audit-load` 诊断，不回退编译或放行就绪。
- 网络回归由 48 扩至 51 项，包括新进程中的缺 DLL、损坏 DLL、与 .NET 10 facade 同目录冷启动成功；安装回归由 46 扩至 54 项；诊断由 9 扩至 12 项，诊断探针也改为固定路径预编译 DLL，同安装目录环境冷加载/JIT 验证且不发包。其他核心、服务、UI、平台、Setup 回归随完整构建执行。
- 2026-09-09 00:25 在现有 Hillstone 连接上，以最终 helper DLL 对用户目标 console.tapsvc.com/nova/ 执行源地址/接口绑定的真实探测：TCP=true、TLS=true、HTTP/1.1 200 OK。只验证探针和对照路径，不算 XD VPN 实网通过；未切换 VPN。
- 本次精确复现的是 0.1.2 的配置失败。代码/安装目录环境对照不等同于真实 VPN 业务验收；本轮仍未切换 Hillstone 或更改实际路由。


## 2026-09-08 · 0.1.2 数据通路与完整审计修复

- 复核及逐项处理记录见 [AUDIT-REVIEW.md](AUDIT-REVIEW.md)。确认修复原生控制唤醒、每次连接资源所有权、失败分类、服务/IPC 重连、路由验证、健康状态和安装权限/回滚；不将 /32+on-link 或企业安全软件判为已证实的断网根因。
- 核心 84 项；服务 Controller 与替代引擎集成 9 项；实际 network.ps1 + Windows PowerShell 5.1 的隔离路由/地址/DNS/清理回归 48 项；诊断脚本离线 9 项；安装事务 46 项；Setup 参数/解包/ACL 描述符 30 项；WPF UI 86 项。
- 平台检查含 8 项数据权限描述符、15 项健康/真实回环 UDP DNS 检查、23 项实际本地 helper 子进程/Job/控制管道生命周期场景，以及消息帧边界、随机命名管道 EOF/读超时/再次连接/持续首实例保护。本地引擎只连接回环拒绝端口，检查密码不回显。
- 原生 OpenConnect 重建后执行 14 项检查：4 种 C/R/EOF/S 控制行为、4 个移除修复的对照、6 种 Wintun 收发错误分类。使用真实 libopenconnect 主循环和替代 TUN，并非真实 VPN 服务器。移除唤醒修复的对照均在 2.5 秒超时；修复后的 C/R/EOF 毫秒级完成，统计请求正常回传。
- 网络 256 路由场景约 3.7–3.9 秒，因此 Windows 的两段 PowerShell 恢复改为固定 65 秒预算，重复 Lost 不延长；尚未实现进程内快路径，也没有实测达成 3 秒恢复。
- 数据状态区分通道建立、正在检查、观察到回包、未验证和配置异常。恢复后的旧流量不视为新回包；检测无回复不会被说成业务成功，也不因 ICMP 被禁误断开。主窗口和托盘一致。
- 新安装器自身提权后才解包到受保护的 Program Files 随机目录；普通进程仅在成功后启动 UI。安装/回滚递归精确重建 DACL，服务在读取 owner.sid 和写日志前独立校验全部数据子对象。真实临时文件 ACL 往返覆盖显式 Everyone ACE 清除，管理员所有权转移仍未执行真机验收。
- 本轮没有停止 Hillstone、切换 VPN、读取密码、提交真实登录或修改实际网络配置。新的 EXE 提升安装/升级、目标业务网址、Wi-Fi 切换和休眠恢复仍待实机验收。之前 r2 的真实升级记录不能替代当前新安装流程验收。


## 2026-09-08 · 安装器修复 r2

- 新增 32 项安装脚本回归检查，使用 Windows PowerShell 5.1 执行真实安装函数，隔离目录及模拟系统服务；包含真实独占文件锁、首次安装、重复升级、账户不匹配、包缺失/损坏、签名失败、路径越界、服务停止/启动失败、ACL 失败、待清理网络会话、文件恢复与服务原状态恢复。
- 本机已实际完成旧版覆盖安装、普通权限→UAC→重复升级，均返回 0；服务 Running，已安装客户端版本 0.1.1.0，未启动客户端或连接 VPN。
- 使用故意损坏的隔离包实际经过 UAC，验证具体错误 Invalid Wintun signature 回传、退出码 1、日志保留，已安装服务保持 Running。
- 完整回归通过：47 核心 + 8 网络 + 68 UI + 32 安装 = 155 项，以及 2 项管道/原生引擎检查。
- 安装前先检查运行中的客户端，升级失败恢复被替换文件及原先运行的服务；全局安装互斥防止并发升级。安装仅复制清单内文件，诊断文件不进入 Program Files。EXE 失败时保留临时目录和日志，界面只显示摘要及日志位置。
- 原截图对应的安装器删除了失败现场，只能确认它隐藏了提权子进程错误，无法追溯原始异常；修复版已通过本机升级，不能据此断言旧次失败的具体原因。首次安装目前为隔离测试，本机实测是升级；未覆盖重启中断、磁盘耗尽、真实 VPN 登录/切网/休眠恢复或其他 Windows 版本。


## 2026-09-08 · Windows UI 0.1.1 预览版

- 当前开发分支为 `codex/windows-support`，已包含 `main` 合并提交 `9f3cbcc`。
- 在 Windows 使用 .NET 10.0.400 完成 WPF 客户端与服务构建。47 项核心测试、8 项网络脚本替身测试、68 项 UI 检查通过；控制管道消息帧、原生引擎启动门控、本地拒绝连接及密码不回显检查通过。
- UI 对照 `Sources/XDVPN/Theme.swift`、`ContentView.swift`、`ConnectionStatusView.swift`、`ProfileView.swift`、`QualityView.swift` 和 `ActivityView.swift`。修复导航/按钮前景色继承、侧栏布局、状态颜色与图标、服务就绪提示、配置引导、成功提示、密码占位/显隐、小屏布局、日志空状态及托盘状态。
- `XDVPN.UI.Tests` 使用独立 Application、真实 WPF 控件和可替换的 `IDesktopServices`，不读取个人配置、凭据，不建立服务连接，不创建托盘图标。覆盖 8 种连接状态、四个页面、780×560/900×620/1060×750 布局、150 DPI 离屏渲染、长配置文本、配置锁定、滚动后保存按钮可达、选中导航白色文字和所有 XAML 绑定。
- 另外覆盖：未配置点击连接转到配置页；偏好保存失败恢复开关；登录启动写入失败回滚；密码保存失败恢复旧凭据；离开页面清空明文与隐藏密码输入；错误关闭后不会因心跳重复弹出；新一次失败仍显示错误；超过 24 小时连接时长；清空日志列表保留质量数据。
- 多尺寸 ICO 含 16/20/24/32/40/48/64/96/128/256 像素，图形来自 macOS `scripts/icon.swift`。应用 EXE、窗口、开始菜单快捷方式和安装 EXE 共用图标；托盘图标按连接状态区分。
- 页面 PNG 位于 `.build/windows-ui-review/`。这是 WPF 离屏渲染验证，并非多显示器真实 DPI 切换验收。当前机器的窗口截图 API 报 `SetIsBorderRequired ... 0x80004002`；可访问性读取正常。
- 构建期间保留用户正在运行的已安装客户端及 VPN 连接。新版安装/升级、物理切网、睡眠恢复仍需要用户实机验收；未自动安装或替换正在运行的版本。
- 本轮从固定哈希的 OpenConnect 9.21 源码完整重建原生运行时，并通过 Wintun 签名和本地启动检查。构建适配修复已同步到源码：Python UTF-8/LF、MinGW 标准头文件、显式网络脚本入口、Windows DLL 路径转换；Windows 分支的 `.gitattributes` 固定 Bash 脚本为 LF。

## 历史记录 · 2026-09-07

日期：2026-09-07。开发/验证主机：macOS Apple Silicon；SDK：隔离安装的 .NET 10.0.400。没有读取实际 VPN 凭据、连接公司 VPN 或更改本机网络。

## 已执行

- Core：47 项测试通过，包括手动断开后切网/唤醒、关闭自动重连、离线暂停、防抖、3 秒恢复期限不延长、取消、冷却、认证/证书/额外认证终止、配置失败恢复、清理失败阻断、过期代次隔离、密码/URL 校验，以及 20,000 次固定随机种子的生命周期顺序检查。
- IPC 消息帧：往返序列化和 3 种非法帧长度检查通过。
- 实际 `network.ps1` 配合内存 NetTCPIP/DNS cmdlet 替身：8 项通过。覆盖正常配置/清理、已有路由保留、服务器出口更新、接口 GUID 复用保护、DNS 归属变化、部分配置回收、服务端字段校验及重复清理。
- PowerShell 7.6.0 解析器检查安装、卸载、构建和网络脚本通过。Windows PowerShell 5.1 兼容性仍需 Windows CI/真机确认。
- .NET Windows targeting：WPF App、Platform、Windows Service 编译成功，无警告、无错误。这是交叉编译检查，不是 Windows 运行验证。
- 原生补丁已成功应用到 SHA-256 固定的 OpenConnect 9.21 源码；检查控制管道、配置成功确认和脚本等待/终止的插入位置；Bash 构建脚本语法通过。

## 已加入 Windows CI，尚未执行

- UCRT64 原生引擎与 DLL 依赖构建、Wintun 哈希及官方签名校验。
- 原生控制管道启动门控、回环拒绝连接、密码不回显检查。
- LocalSystem 服务安装、管道内核所有者验证、状态握手、拒绝无效配置，以及安全停止/卸载。
- 自包含 Windows x64 ZIP 打包。

## 真机验收清单

| 场景 | 预期 | 状态 |
| --- | --- | --- |
| 普通用户安装、首次连接 | UAC 仅在安装，日常无需管理员权限 | 待验收 |
| WPF 100%/150%/200% 缩放 | 四个页面、托盘、键盘操作正常 | 待验收 |
| 正常连接、内网 DNS 和业务访问 | 分配地址/路由/DNS 与服务器配置一致 | 待验收 |
| Wi-Fi 中断 20 秒后恢复 | 离线不重试，恢复后顺序重建 | 待验收 |
| Wi-Fi A→B / 有线→无线 | 更新服务器出口，单一隧道恢复 | 待验收 |
| 短睡眠 / 长睡眠 / 休眠 | 唤醒后自动恢复，无旧路由残留 | 待验收 |
| 登录中取消 / 恢复中取消 | 不再启动新引擎，清理最终完成 | 待验收 |
| 密码错误 / 不受信任证书 / MFA | 停止重试并提示，避免反复错误登录 | 待验收 |
| 杀死 UI / 服务 / 引擎 | 按所有权停止子进程并恢复网络 | 待验收 |
| 断开及卸载 | 只删除本次配置，物理网络保持可用 | 待验收 |

这些未执行项不以源码实现或替身测试代替结论；当前产物定位为待 Windows 验收的预览版。
