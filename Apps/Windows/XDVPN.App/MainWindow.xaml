<Window x:Class="XDVPN.App.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:XDVPN.App" Title="XD VPN" Icon="pack://application:,,,/XDVPN.App;component/Assets/XDVPN.ico" Width="1060" Height="800" MinWidth="780" MinHeight="560" Background="{StaticResource CanvasBrush}" Foreground="{StaticResource Ink}" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="13" WindowStartupLocation="CenterScreen" Closing="WindowClosing" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Grid x:Name="RootLayout" Background="{StaticResource CanvasBrush}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition x:Name="SidebarColumn" Width="204" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Border Background="#192D27">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0,36,0,0">
          <StackPanel Orientation="Horizontal" Margin="22,0,0,0">
            <Image Source="pack://application:,,,/XDVPN.App;component/Assets/XDVPN.png" Width="44" Height="44" RenderOptions.BitmapScalingMode="HighQuality" />
            <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
              <TextBlock Text="XD VPN" Foreground="White" FontSize="20" FontWeight="SemiBold" />
              <TextBlock Text="YOUR PRIVATE LINK" Foreground="#A4B4AA" FontFamily="Consolas" FontSize="8" Margin="0,3,0,0" />
            </StackPanel>
          </StackPanel>
          <TextBlock Text="工作空间" Foreground="#A4B4AA" FontSize="11" Margin="26,40,0,12" />
          <RadioButton x:Name="ConnectionNav" Style="{StaticResource Nav}" Tag="Connection" IsChecked="True" Checked="Navigate" AutomationProperties.Name="网络连接">
            <StackPanel Orientation="Horizontal">
              <Path Style="{StaticResource VectorIcon}" Data="M12,2 L12,11 M6,5 A9,9 0 1 0 18,5" />
              <TextBlock Text="网络连接" Margin="12,0,0,0" />
            </StackPanel>
          </RadioButton>
          <RadioButton x:Name="QualityNav" Style="{StaticResource Nav}" Tag="Quality" Checked="Navigate" AutomationProperties.Name="连接质量">
            <StackPanel Orientation="Horizontal">
              <Path Style="{StaticResource VectorIcon}" Data="M3,19 L3,13 M10,19 L10,8 M17,19 L17,2" />
              <TextBlock Text="连接质量" Margin="12,0,0,0" />
            </StackPanel>
          </RadioButton>
          <RadioButton x:Name="ProfileNav" Style="{StaticResource Nav}" Tag="Profile" Checked="Navigate" AutomationProperties.Name="VPN 配置">
            <StackPanel Orientation="Horizontal">
              <Path Style="{StaticResource VectorIcon}" Data="M2,6 L22,6 M2,17 L22,17 M7,2 L7,10 M16,13 L16,21" />
              <TextBlock Text="VPN 配置" Margin="12,0,0,0" />
            </StackPanel>
          </RadioButton>
          <RadioButton x:Name="ActivityNav" Style="{StaticResource Nav}" Tag="Activity" Checked="Navigate" AutomationProperties.Name="连接日志">
            <StackPanel Orientation="Horizontal">
              <Path Style="{StaticResource VectorIcon}" Data="M3,4 L21,4 M3,11 L21,11 M3,18 L15,18" />
              <TextBlock Text="连接日志" Margin="12,0,0,0" />
            </StackPanel>
          </RadioButton>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="24,20,22,22">
          <StackPanel x:Name="SidebarMessage">
            <Path Data="M2,20 C0,4 13,0 23,2 C23,18 12,24 2,20 M2,20 L17,7" Stroke="#B5D8C1" StrokeThickness="1.3" Stretch="Uniform" Width="24" Height="25" HorizontalAlignment="Left" />
            <TextBlock Text="少一点操作，&#xA;多一点专注。" FontSize="15" LineHeight="27" Foreground="#D3DDD7" Margin="0,15,0,8" />
            <TextBlock Text="让连接安静地发生。" FontSize="11" Foreground="#A4B4AA" />
            <Border Height="1" Background="#35463E" Margin="0,25,0,18" />
          </StackPanel>
          <StackPanel Orientation="Horizontal">
            <Ellipse Width="5" Height="5" Fill="{Binding StatusColor}" Margin="0,0,7,0" />
            <TextBlock Text="{Binding ServiceSummary}" Foreground="#A4B4AA" FontSize="10" />
          </StackPanel>
          <TextBlock Text="{Binding VersionLabel}" Foreground="#A4B4AA" FontFamily="Consolas" FontSize="9" Margin="12,7,0,0" />
        </StackPanel>
      </Grid>
    </Border>
    <Grid x:Name="MainContent" Grid.Column="1" Margin="32,30,32,22" SizeChanged="ContentSizeChanged">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid Margin="0,0,0,22">
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock x:Name="Eyebrow" Text="A LITTLE CLOSER TO WORK" Style="{StaticResource EyebrowStyle}" />
          <TextBlock x:Name="PageTitle" Text="工作网络，一键就绪。" FontSize="27" FontWeight="SemiBold" Margin="0,7,10,0" />
        </StackPanel>
        <Border Grid.Column="1" CornerRadius="16" BorderBrush="{StaticResource Line}" BorderThickness="1" Background="#CCFFFFFF" Padding="12,8" VerticalAlignment="Center">
          <StackPanel Orientation="Horizontal">
            <Path Data="M2,2 L20,2 L20,15 L2,15 Z M0,18 L22,18" Stroke="{StaticResource Muted}" StrokeThickness="1.3" Width="15" Height="14" Stretch="Uniform" Margin="0,0,7,0" />
            <TextBlock Text="此 Windows PC" Style="{StaticResource Caption}" FontSize="11" />
          </StackPanel>
        </Border>
      </Grid>
      <Border Grid.Row="1" Background="#FFF1DD" CornerRadius="12" Padding="13" Margin="0,0,0,16" Visibility="{Binding HasIssue, Converter={StaticResource BoolVisibility}}">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="22" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="!" Foreground="#946B37" FontWeight="Bold" />
          <TextBlock Grid.Column="1" Text="{Binding Issue}" Foreground="#946B37" FontSize="12" VerticalAlignment="Center" />
          <Button Grid.Column="2" Click="DismissIssue" Style="{StaticResource LinkButton}" Margin="12,0,0,0" AutomationProperties.Name="关闭提示">
            <Path Data="M2,2 L12,12 M12,2 L2,12" Style="{StaticResource VectorIcon}" Width="12" Height="12" Stroke="#946B37" />
          </Button>
        </Grid>
      </Border>
      <ScrollViewer x:Name="PageScroll" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,4,0" PanningMode="VerticalOnly">
        <Grid>
          <StackPanel x:Name="ConnectionPage">
            <Border x:Name="ServiceBanner" Style="{StaticResource Card}" Padding="16" Margin="0,0,0,18" Visibility="{Binding NeedsServiceAttention, Converter={StaticResource BoolVisibility}}">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="{Binding ServiceMessage}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding ServiceHint}" Style="{StaticResource Caption}" Margin="0,5,14,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="重新检测" Style="{StaticResource Secondary}" Click="RefreshService" VerticalAlignment="Center" />
              </Grid>
            </Border>
            <Grid x:Name="DashboardColumns">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition x:Name="DashboardGap" Width="18" />
                <ColumnDefinition x:Name="ProfileColumn" Width="226" />
              </Grid.ColumnDefinitions>
              <Border x:Name="ConnectionCard" Style="{StaticResource Card}" Padding="22" Background="{Binding StatusSurface}">
                <StackPanel>
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="CONNECTION" Style="{StaticResource EyebrowStyle}" VerticalAlignment="Center" />
                    <Border Grid.Column="1" Background="{Binding StatusColor}" CornerRadius="12" Padding="10,5">
                      <TextBlock Text="{Binding StatusLabel}" Foreground="White" FontSize="11" FontWeight="SemiBold" AutomationProperties.Name="VPN 状态" />
                    </Border>
                  </Grid>
                  <Grid Height="176" Margin="0,4,0,0" IsHitTestVisible="False">
                    <Ellipse Width="146" Height="146" Stroke="{Binding StatusColor}" Opacity="0.17" StrokeDashArray="3 5" />
                    <Ellipse Width="118" Height="118" Stroke="{Binding StatusColor}" Opacity="0.1" />
                    <Ellipse Width="98" Height="98" Fill="{Binding OrbitBackground}" />
                    <Path x:Name="ProgressArc" Data="M22,73 A51,51 0 0 1 73,22" Width="146" Height="146" Stroke="{Binding StatusColor}" StrokeThickness="2.5" StrokeStartLineCap="Round" RenderTransformOrigin="0.5,0.5" Visibility="{Binding IsProgress, Converter={StaticResource BoolVisibility}}">
                      <Path.RenderTransform>
                        <RotateTransform x:Name="OrbitRotation" />
                      </Path.RenderTransform>
                    </Path>
                    <Path x:Name="StateIcon" Data="{Binding StatusGlyph}" Stroke="{Binding OrbitForeground}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform" Width="40" Height="40" />
                  </Grid>
                  <TextBlock Text="{Binding Title}" Foreground="{Binding StatusColor}" FontSize="23" FontWeight="SemiBold" TextAlignment="Center" />
                  <TextBlock Text="{Binding Subtitle}" Style="{StaticResource Caption}" TextAlignment="Center" MinHeight="38" Margin="0,6,0,17" />
                  <Button x:Name="PrimaryAction" IsEnabled="{Binding CanAct}" Click="ConnectionAction" MaxWidth="216" HorizontalAlignment="Stretch" MinHeight="46" Background="{Binding ActionBackground}" Foreground="{Binding ActionForeground}" BorderBrush="#CCD5CF" AutomationProperties.Name="{Binding ActionTitle}">
                    <StackPanel Orientation="Horizontal">
                      <Path Style="{StaticResource VectorIcon}" Data="{Binding ActionGlyph}" Width="15" Height="15" Margin="0,0,9,0" />
                      <TextBlock Text="{Binding ActionTitle}" />
                    </StackPanel>
                  </Button>
                  <TextBlock Text="{Binding TrafficSummary}" Style="{StaticResource Caption}" FontSize="11" TextAlignment="Center" Margin="0,14,0,0" Visibility="{Binding HasConnectivity, Converter={StaticResource BoolVisibility}}" />
                  <Border Height="1" Background="{StaticResource Line}" Margin="0,24,0,17" />
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition />
                      <ColumnDefinition Width="22" />
                      <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                      <TextBlock Text="连接时长" Style="{StaticResource Caption}" FontSize="11" />
                      <TextBlock Text="{Binding Duration}" FontSize="14" Margin="0,6,0,0" FontFamily="Consolas" />
                    </StackPanel>
                    <Border Grid.Column="1" Width="1" Height="28" Background="{StaticResource Line}" />
                    <StackPanel Grid.Column="2">
                      <TextBlock Text="分配地址" Style="{StaticResource Caption}" FontSize="11" />
                      <TextBlock Text="{Binding Address}" ToolTip="{Binding Address}" FontSize="14" Margin="0,6,0,0" FontFamily="Consolas" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </Grid>
                </StackPanel>
              </Border>
              <StackPanel x:Name="ProfileSummary" Grid.Column="2">
                <Border Style="{StaticResource Card}" Padding="20">
                  <StackPanel>
                    <TextBlock Text="MY PROFILE" Style="{StaticResource EyebrowStyle}" />
                    <Border Background="{StaticResource CanvasBrush}" CornerRadius="12" Width="42" Height="42" HorizontalAlignment="Left" Margin="0,20,0,12">
                      <Path Data="M2,22 L2,7 L12,7 L12,22 M12,22 L12,2 L22,2 L22,22 M0,22 L24,22 M6,11 L8,11 M6,15 L8,15 M16,6 L18,6 M16,11 L18,11 M16,16 L18,16" Stroke="{StaticResource Green}" StrokeThickness="1.4" Stretch="Uniform" Width="23" Height="23" />
                    </Border>
                    <TextBlock Text="{Binding ProfileName}" ToolTip="{Binding ProfileName}" FontSize="17" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding ProfileServer}" ToolTip="{Binding ProfileServer}" Style="{StaticResource Caption}" FontSize="11" Margin="0,6,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    <Border Height="1" Background="{StaticResource Line}" Margin="0,17" />
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="46" />
                        <ColumnDefinition />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="协议" Style="{StaticResource Caption}" FontSize="11" />
                      <TextBlock Grid.Column="1" Text="AnyConnect" FontSize="11" HorizontalAlignment="Right" />
                    </Grid>
                    <Grid Margin="0,12,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="46" />
                        <ColumnDefinition />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="账号" Style="{StaticResource Caption}" FontSize="11" />
                      <TextBlock Grid.Column="1" Text="{Binding ProfileUsername}" ToolTip="{Binding ProfileUsername}" FontSize="11" TextAlignment="Right" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    </Grid>
                    <Button Content="{Binding ProfileAction}" Click="OpenProfile" Style="{StaticResource LinkButton}" Margin="0,17,0,0" />
                  </StackPanel>
                </Border>
                <Border Background="#E8EFE5" CornerRadius="18" Padding="20" Margin="0,18,0,0">
                  <StackPanel>
                    <Path Data="M9,3 A6,6 0 1 0 9,15 A6,6 0 1 0 9,3 M15,9 L27,9 M22,9 L22,14 M27,9 L27,13" Stroke="{StaticResource Green}" StrokeThickness="1.5" Stretch="Uniform" Width="26" Height="21" HorizontalAlignment="Left" />
                    <TextBlock Text="密码，安心记住。" Foreground="{StaticResource Green}" FontWeight="SemiBold" FontSize="14" Margin="0,12,0,8" />
                    <TextBlock Text="保存在 Windows 凭据管理器中，下次连接不用再输入。" Foreground="#466B56" FontSize="12" LineHeight="20" />
                  </StackPanel>
                </Border>
              </StackPanel>
            </Grid>
            <Border Background="#EDF1E9" BorderBrush="#E1E8DD" BorderThickness="1" CornerRadius="17" Padding="19" Margin="0,18,0,0">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="55" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>
                <Border Width="40" Height="40" CornerRadius="12" Background="White" HorizontalAlignment="Left" VerticalAlignment="Center">
                  <Path Data="M20,9 A8,8 0 1 0 20,16 M20,3 L20,9 L14,9" Stroke="{StaticResource Green}" StrokeThickness="1.6" Stretch="Uniform" Width="22" Height="22" />
                </Border>
                <StackPanel Grid.Column="1">
                  <WrapPanel>
                    <TextBlock Text="自动连接" FontWeight="SemiBold" FontSize="13" Margin="0,0,9,0" />
                    <TextBlock Text="AUTO CONNECT" Style="{StaticResource EyebrowStyle}" FontSize="9" VerticalAlignment="Center" />
                  </WrapPanel>
                  <TextBlock Text="启动时连接、掉线后重试；手动断开后保持断开。" Style="{StaticResource Caption}" FontSize="11" Margin="0,5,0,0" />
                </StackPanel>
                <CheckBox x:Name="AutoToggle" Grid.Column="2" Style="{StaticResource Switch}" HorizontalAlignment="Right" IsChecked="{Binding AutoConnect, Mode=OneWay}" IsEnabled="{Binding CanChangePreferences}" Click="AutoChanged" AutomationProperties.Name="Auto Connect 自动连接" ToolTip="仅修改自动连接偏好，不会立即连接或断开" />
              </Grid>
            </Border>
            <Border Background="#EDF1E9" BorderBrush="#E1E8DD" BorderThickness="1" CornerRadius="17" Padding="19" Margin="0,14,0,0">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="登录后启动" FontWeight="SemiBold" />
                  <TextBlock Text="登录 Windows 后，在托盘中准备好工作网络。" Style="{StaticResource Caption}" FontSize="11" Margin="0,5,0,0" />
                </StackPanel>
                <CheckBox x:Name="StartupToggle" Grid.Column="1" Style="{StaticResource Switch}" HorizontalAlignment="Right" IsChecked="{Binding LaunchAtLogin, Mode=OneWay}" Click="StartupChanged" AutomationProperties.Name="登录后启动" />
              </Grid>
            </Border>
            <TextBlock Text="凭据留在你的电脑，连接交给 XD VPN。" Style="{StaticResource Caption}" FontSize="10" Margin="2,16,0,4" />
          </StackPanel>
          <StackPanel x:Name="ProfilePage" Visibility="Collapsed">
            <TextBlock Text="一个配置，连接你的日常。" Style="{StaticResource Caption}" Margin="0,0,0,18" />
            <Border Style="{StaticResource Card}">
              <StackPanel>
                <Grid Margin="0,0,0,22">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel>
                    <TextBlock Text="连接信息" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="使用与你的 Cisco AnyConnect 相同的账号。" Style="{StaticResource Caption}" Margin="0,5,10,0" />
                  </StackPanel>
                  <Border Grid.Column="1" Background="#EAF3EC" CornerRadius="12" Padding="10,6" VerticalAlignment="Top">
                    <TextBlock Text="AnyConnect" Foreground="{StaticResource Green}" FontSize="11" />
                  </Border>
                </Grid>
                <Border Background="#EDF1E9" CornerRadius="9" Padding="12" Margin="0,0,0,16">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Visibility" Value="Collapsed" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding CanEdit}" Value="False">
                          <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="连接期间配置已锁定。断开或取消连接后即可编辑。" Style="{StaticResource Caption}" />
                </Border>
                <StackPanel x:Name="ProfileFields" IsEnabled="{Binding CanEdit}">
                  <TextBlock Text="配置名称" />
                  <TextBox x:Name="ProfileName" AutomationProperties.Name="配置名称" MaxLength="1024" />
                  <WrapPanel>
                    <TextBlock Text="VPN 服务器" />
                    <TextBlock Text="支持地址、端口和路径" Style="{StaticResource Caption}" Margin="10,0,0,0" />
                  </WrapPanel>
                  <TextBox x:Name="Server" AutomationProperties.Name="VPN 服务器" MaxLength="1024" />
                  <TextBlock Text="用户名" />
                  <TextBox x:Name="Username" AutomationProperties.Name="VPN 用户名" MaxLength="1024" />
                  <WrapPanel>
                    <TextBlock Text="VPN 密码" />
                    <TextBlock Text="{Binding PasswordHint}" Style="{StaticResource Caption}" FontSize="11" Margin="10,0,0,0" />
                  </WrapPanel>
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition />
                      <ColumnDefinition Width="60" />
                    </Grid.ColumnDefinitions>
                    <Grid>
                      <PasswordBox x:Name="Password" AutomationProperties.Name="VPN 密码" MaxLength="2048" PasswordChanged="PasswordChanged" />
                      <TextBox x:Name="RevealedPassword" Visibility="Collapsed" AutomationProperties.Name="显示的 VPN 密码" MaxLength="2048" TextChanged="RevealedPasswordChanged" />
                      <TextBlock x:Name="PasswordPlaceholder" Text="输入 VPN 密码" Foreground="{StaticResource Muted}" Margin="13,19,0,0" VerticalAlignment="Top" IsHitTestVisible="False" />
                    </Grid>
                    <Button x:Name="RevealButton" Grid.Column="1" Content="显示" Click="TogglePassword" Style="{StaticResource LinkButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8,12,0,0" AutomationProperties.Name="显示密码" />
                  </Grid>
                  <Expander Header="高级 · 认证组（可选）" Margin="0,0,0,18">
                    <StackPanel Margin="0,12,0,0">
                      <TextBlock Text="仅在服务器要求时填写认证组。" Style="{StaticResource Caption}" />
                      <TextBox x:Name="AuthGroup" AutomationProperties.Name="认证组" MaxLength="1024" Margin="0,8,0,0" />
                    </StackPanel>
                  </Expander>
                  <TextBlock Text="更换服务器、账号或认证组后，需要重新填写密码。" Style="{StaticResource Caption}" FontSize="11" />
                  <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Content="忘记已保存密码" Click="ForgetPassword" Style="{StaticResource LinkButton}" Foreground="{StaticResource Muted}" FontSize="12" Visibility="{Binding HasPassword, Converter={StaticResource BoolVisibility}}" />
                    <Button x:Name="SaveProfileButton" Grid.Column="1" Content="保存配置" Click="SaveProfile" Width="150" />
                  </Grid>
                </StackPanel>
              </StackPanel>
            </Border>
            <TextBlock Text="密码只保存在本机。保存后返回连接页，日常连接无需重复授权。公司 VPN 可能仅允许从办公网以外连接。" Style="{StaticResource Caption}" FontSize="11" Margin="2,18,2,0" />
            <Border Style="{StaticResource Card}" Padding="18" Margin="0,18,0,0">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="系统服务" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding ServiceMessage}" Style="{StaticResource Caption}" Margin="0,6,12,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="重新检测" Click="RefreshService" Style="{StaticResource Secondary}" />
              </Grid>
            </Border>
          </StackPanel>
          <StackPanel x:Name="QualityPage" Visibility="Collapsed">
            <Grid Margin="0,0,0,18">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="{Binding QualityNote}" Style="{StaticResource Caption}" VerticalAlignment="Center" />
              <Button Grid.Column="1" Content="查看日志" Click="OpenActivity" Style="{StaticResource LinkButton}" Margin="12,0,0,0" />
            </Grid>
            <UniformGrid x:Name="QualityMetrics" Columns="3">
              <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                  <TextBlock Text="连接成功率" Style="{StaticResource Caption}" />
                  <TextBlock Text="{Binding SuccessRate}" FontSize="28" FontWeight="SemiBold" Margin="0,12,0,10" />
                  <TextBlock Text="{Binding SuccessDetail}" Style="{StaticResource Caption}" FontSize="11" />
                </StackPanel>
              </Border>
              <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                  <TextBlock Text="连接耗时 P95" Style="{StaticResource Caption}" />
                  <TextBlock Text="{Binding P95}" FontSize="26" FontWeight="SemiBold" Margin="0,12,0,10" />
                  <TextBlock Text="{Binding SampleDetail}" Style="{StaticResource Caption}" FontSize="11" />
                </StackPanel>
              </Border>
              <Border Style="{StaticResource Card}" Padding="16">
                <StackPanel>
                  <TextBlock Text="恢复次数" Style="{StaticResource Caption}" />
                  <TextBlock Text="{Binding RecoveryCount}" FontSize="28" FontWeight="SemiBold" Margin="0,12,0,10" />
                  <TextBlock Text="{Binding RecoveryDetail}" Style="{StaticResource Caption}" FontSize="11" />
                </StackPanel>
              </Border>
            </UniformGrid>
            <Border Style="{StaticResource Card}" Margin="0,18,0,0" Padding="20">
              <StackPanel>
                <TextBlock Text="连接耗时 · 成功样本" FontSize="14" FontWeight="SemiBold" />
                <local:LatencyChart Samples="{Binding LatencySamples}" Height="142" Margin="0,16,0,8" AutomationProperties.Name="最近 24 小时连接耗时图" />
                <TextBlock Text="{Binding CancellationNote}" Style="{StaticResource Caption}" FontSize="11" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,18,0,0">
              <StackPanel>
                <TextBlock Text="最近的连接" FontSize="16" FontWeight="SemiBold" />
                <StackPanel Margin="0,32">
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Setter Property="Visibility" Value="Visible" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding HasSamples}" Value="True">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>
                  <TextBlock Text="暂无完成的连接记录" FontSize="14" TextAlignment="Center" />
                  <TextBlock Text="连接完成后，成功与失败的记录会显示在这里。" Style="{StaticResource Caption}" TextAlignment="Center" Margin="0,8,0,0" />
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Samples}" Margin="0,16,0,0">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="0,12">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="80" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="{Binding Time}" FontFamily="Consolas" FontSize="12" />
                          <TextBlock Grid.Column="1" Text="{Binding Duration}" TextAlignment="Right" FontSize="12" />
                          <TextBlock Grid.Column="2" Text="{Binding Result}" Foreground="{Binding Color}" TextAlignment="Right" FontSize="12" />
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
            <TextBlock Text="统计来自本机保留日志，日志轮转后可能不足 24 小时。隧道建立不等于业务可用；本页暂未采集业务延迟和丢包，不上传到服务器。" Style="{StaticResource Caption}" FontSize="11" Margin="2,18,2,4" />
          </StackPanel>
          <Grid x:Name="ActivityPage" Visibility="Collapsed">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <WrapPanel Margin="0,0,0,16">
              <Button Content="复制日志" Click="CopyLogs" IsEnabled="{Binding HasEvents}" Style="{StaticResource Secondary}" Margin="0,0,10,6" />
              <Button Content="清空本次列表" Click="ClearLogs" IsEnabled="{Binding HasEvents}" Style="{StaticResource Secondary}" Margin="0,0,10,6" />
              <Button Content="打开日志目录" Click="OpenLogs" Style="{StaticResource Secondary}" Margin="0,0,0,6" />
            </WrapPanel>
            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition />
                </Grid.RowDefinitions>
                <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="20">
                  <Grid>
                    <TextBlock Text="CONNECTION JOURNAL" Style="{StaticResource EyebrowStyle}" />
                    <TextBlock Text="{Binding EventCount}" Style="{StaticResource Caption}" HorizontalAlignment="Right" />
                  </Grid>
                </Border>
                <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="20,60">
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Setter Property="Visibility" Value="Visible" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding HasEvents}" Value="True">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>
                  <TextBlock Text="这里很安静" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" />
                  <TextBlock Text="下一次连接的动态会显示在这里。" Style="{StaticResource Caption}" TextAlignment="Center" Margin="0,10,0,0" />
                </StackPanel>
                <ListBox x:Name="ActivityList" Grid.Row="1" ItemsSource="{Binding Events}" BorderThickness="0" Background="Transparent" Foreground="{StaticResource Ink}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" Visibility="{Binding HasEvents, Converter={StaticResource BoolVisibility}}">
                  <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                      <Setter Property="Padding" Value="0" />
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                  </ListBox.ItemContainerStyle>
                  <ListBox.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="18,13">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="72" />
                            <ColumnDefinition Width="18" />
                            <ColumnDefinition />
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="{Binding Time}" FontFamily="Consolas" Foreground="{StaticResource Muted}" FontSize="11" />
                          <Ellipse Grid.Column="1" Width="5" Height="5" Fill="{Binding Color}" VerticalAlignment="Top" Margin="0,5,0,0" />
                          <TextBlock Grid.Column="2" Text="{Binding Message}" FontSize="12" LineHeight="20" />
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ListBox.ItemTemplate>
                </ListBox>
              </Grid>
            </Border>
            <TextBlock Grid.Row="2" Text="日志保存在本机。清空列表不会删除文件日志；不记录密码、Cookie 或原始认证响应。" Style="{StaticResource Caption}" FontSize="11" Margin="2,16,2,0" />
          </Grid>
        </Grid>
      </ScrollViewer>
    </Grid>
    <Border Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Background="{StaticResource Ink}" CornerRadius="22" Padding="20,12" Margin="20,0,20,24" Visibility="{Binding HasToast, Converter={StaticResource BoolVisibility}}" IsHitTestVisible="False">
      <TextBlock Text="{Binding Toast}" Foreground="White" FontSize="12" />
    </Border>
  </Grid>
</Window>